function translator(str){	var vow = /[aeiou]/gi; //global and case intensive search	var pig = "";	for (var i in str){		if(str[0].match(vow)){			pig = str + "way";		}else {			var place = str.indexOf(str.match(vow)[0]);			pig = str.substr(place) + str.substr(0, place) + "ay";		}	}	return pig;}console.log(translator("consonant"));